apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mydocker.fullname" . }}-test-connection"
  labels:
    {{- include "mydocker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mydocker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
