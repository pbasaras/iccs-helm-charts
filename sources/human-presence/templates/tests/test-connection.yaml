apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "human-presence.fullname" . }}-test-connection"
  labels:
    {{- include "human-presence.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "human-presence.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
