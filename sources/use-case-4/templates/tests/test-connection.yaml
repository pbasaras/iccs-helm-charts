apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "use-case-4.fullname" . }}-test-connection"
  labels:
    {{- include "use-case-4.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "use-case-4.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
