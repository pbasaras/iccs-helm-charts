apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-jetson.fullname" . }}-test-connection"
  labels:
    {{- include "test-jetson.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-jetson.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
