apiVersion: v1
kind: Service
metadata:
  name: {{ include "hp-jetson.fullname" . }}
  labels:
    {{- include "hp-jetson.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hp-jetson.selectorLabels" . | nindent 4 }}
