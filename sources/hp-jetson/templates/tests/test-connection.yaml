apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hp-jetson.fullname" . }}-test-connection"
  labels:
    {{- include "hp-jetson.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hp-jetson.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
